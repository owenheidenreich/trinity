# SECURITY: Pin exact versions to prevent supply chain attacks
# and ensure reproducible builds. Update these periodically with security audits.
flask==3.0.3
flask-cors==4.0.0
flask-compress==1.15
requests==2.31.0
urllib3==2.2.1
psutil==5.9.8
APScheduler==3.10.4
pycryptodome==3.20.0
python-dotenv==1.0.1
cryptography==42.0.5
cachetools==5.3.3
yfinance==0.2.37
feedparser==6.0.11
beautifulsoup4==4.12.3
argon2-cffi==23.1.0

# ===== LLM INTELLIGENCE UPGRADES (v4.0 → v5.0 memory rewrite) =====
# RAG: FastEmbed for embeddings (ONNX, CPU-friendly, ~100MB)
fastembed==0.7.4
# Vector search: sqlite-vec ANN (replaces brute-force Python UDF)
sqlite-vec==0.1.6
# Whole-DB encryption: sqlcipher via pysqlcipher3 (replaces field-level AES)
pysqlcipher3==1.2.0
# Code execution: Sandboxed Python (blocks imports, file, network)
RestrictedPython==8.1
# Numpy for vector operations
numpy==1.26.4

# ===== OBSERVABILITY (Phase 2A) =====
# Added: February 5, 2026 - Prometheus metrics for production monitoring
prometheus-client==0.20.0

# ===== MCP (Model Context Protocol) =====
# Added: February 12, 2026 - Expose Trinity tools via MCP, connect to external MCP servers
# Optional: graceful degradation if not installed (stdio server requires it, HTTP route does not)
mcp>=1.0.0

# ===== TESTING INFRASTRUCTURE (Phase 1) =====
# Added: February 5, 2026
pytest==8.0.0
pytest-cov==4.1.0
pytest-asyncio==0.23.0
pytest-mock==3.12.0
pytest-timeout==2.2.0
responses==0.25.0           # HTTP mocking
faker==22.0.0               # Test data generation
freezegun==1.4.0            # Time mocking
factory-boy==3.3.0          # Object factories