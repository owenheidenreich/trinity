version: "2.0"

# ============================================================================
# Trinity Production: Qwen3 32B
# ============================================================================
#
# The production deployment. This is the only tier that matters.
#
# Model: Qwen3 32B — Q4_K_M quantized, 128K native context (64K active)
# Cost: ~$0.80-1.50/hour (~$600-1000/month)
# Response Time: 5-20 seconds
# GPU: Requires 40GB+ VRAM — A100, A6000, H100, L40S, A40, or RTX 4090
#      RTX 3090 (24GB) is TOO SMALL — spills layers to CPU, >60s TTFT
#
# Deploy:
#   ./scripts/trinity-deploy-production.sh production
# ============================================================================

services:
  trinity:
    image: gdubx/trinity-inference:v2-20260217-190654
    env:
      - PROVIDER_ID=trinity-production-qwen3-32b
      - MODEL_NAME=qwen3:32b
      - MODEL_BACKEND=ollama
      - GPU_TYPE=A100
      - MAX_QUEUE_SIZE=5
      - OLLAMA_HOST=http://localhost:11434
      # RAG & Semantic Memory
      - EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
      - RAG_TOP_K=5
      - RAG_CHUNK_SIZE=512
      - RAG_CHUNK_OVERLAP=50
      - WORKING_MEMORY_SIZE=5
      - SEMANTIC_MEMORY_SIZE=8
      - PROFILE_TOKEN_BUDGET=2500
      - RECENCY_WEIGHT=0.3
      # Tool Use & Code Execution
      - CODE_EXECUTION_ENABLED=false
      - CODE_EXECUTION_TIMEOUT=5
      # API Keys - Injected from .env at deployment time
      - LIGHTHOUSE_API_KEY=${LIGHTHOUSE_API_KEY}
      - BRAVE_SEARCH_API_KEY=${BRAVE_SEARCH_API_KEY}
      # Deployment tracking
      - DEPLOYMENT_TIER=production
      - DEPLOYMENT_TIER_NAME=Production
      - HOURLY_COST_AKT=1.00
      - DAILY_COST_AKT=24.0
      - SESSION_TYPE=community
    params:
      storage:
        models:
          mount: /home/trinity/.ollama
          readMode: false
    expose:
      - port: 8000
        as: 80
        to:
          - global: true
        http_options:
          read_timeout: 60000
          send_timeout: 60000
          max_body_size: 52428800

profiles:
  compute:
    trinity:
      resources:
        cpu:
          units: 8
        memory:
          size: 48Gi
        storage:
          - size: 10Gi
          - name: models
            size: 80Gi
            attributes:
              persistent: true
              class: beta3
        gpu:
          units: 1
          attributes:
            vendor:
              nvidia:
                - model: a100
                - model: a6000
                - model: h100
                - model: l40s
                - model: a40
                - model: rtx4090

  placement:
    akash:
      pricing:
        trinity:
          denom: uakt
          amount: 100000

deployment:
  trinity:
    akash:
      profile: trinity
      count: 1
