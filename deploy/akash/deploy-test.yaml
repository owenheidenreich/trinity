version: "2.0"

# ============================================================================
# Trinity Test: Qwen3 8B
# ============================================================================
#
# Cheap deploy for smoke-testing before committing to production GPU costs.
# Same model family as production (qwen3), just smaller.
#
# Model: Qwen3 8B (~5GB)
# Cost: ~$0.05-0.15/hour (~$40-100/month)
# GPU: Any NVIDIA GPU (T4, RTX 3090, 4090, A10, etc.)
#
# Deploy:
#   ./scripts/trinity-deploy-production.sh test
# ============================================================================

services:
  trinity:
    image: gdubx/trinity-inference:v2-20260225-202034
    env:
      - PROVIDER_ID=trinity-test-qwen3-8b
      - MODEL_NAME=qwen3:8b
      - MODEL_BACKEND=llama-server
      - LLAMA_SERVER_CHAT_PORT=8081
      - LLAMA_SERVER_INGEST_PORT=8082
      - CHAT_MODEL=qwen3:8b
      - INGEST_MODEL=qwen3:8b
      - GPU_TYPE=auto
      - CHATS_DIR=/data/chats
      - TZ=UTC
      - MAX_QUEUE_SIZE=10
      # RAG & Semantic Memory
      - EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
      - RAG_TOP_K=5
      - RAG_CHUNK_SIZE=512
      - RAG_CHUNK_OVERLAP=50
      - WORKING_MEMORY_SIZE=5
      - SEMANTIC_MEMORY_SIZE=8
      - PROFILE_TOKEN_BUDGET=2500
      - PROFILE_MAX_FACTS=15
      - RECENCY_WEIGHT=0.3
      # Tool Use & Code Execution
      - CODE_EXECUTION_ENABLED=false
      - CODE_EXECUTION_TIMEOUT=5
      # API Keys - Injected from .env at deployment time
      - LIGHTHOUSE_API_KEY=${LIGHTHOUSE_API_KEY}
      - BRAVE_SEARCH_API_KEY=${BRAVE_SEARCH_API_KEY}
      - HF_TOKEN=${HF_TOKEN}
      # Deployment tracking
      - DIAGNOSTIC_ENABLED=true
      - DEPLOYMENT_TIER=test
      - DEPLOYMENT_TIER_NAME=Test
      - HOURLY_COST_AKT=0.15
      - DAILY_COST_AKT=3.6
      - SESSION_TYPE=community
    params:
      storage:
        models:
          mount: /home/trinity/.models
          readMode: false
    expose:
      - port: 8000
        as: 80
        to:
          - global: true
        http_options:
          read_timeout: 60000
          send_timeout: 60000
          max_body_size: 52428800

profiles:
  compute:
    trinity:
      resources:
        cpu:
          units: 4
        memory:
          size: 16Gi
        storage:
          - size: 10Gi
          - name: models
            size: 30Gi
            attributes:
              persistent: true
              class: beta3
        gpu:
          units: 1
          attributes:
            vendor:
              nvidia:

  placement:
    akash:
      pricing:
        trinity:
          denom: uakt
          amount: 100000

deployment:
  trinity:
    akash:
      profile: trinity
      count: 1
