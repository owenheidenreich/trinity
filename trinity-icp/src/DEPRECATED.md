# DEPRECATED — Legacy Frontend

This directory contains the **vanilla JS frontend (v2.8.0)**.

It has been replaced by the **React 19 + TypeScript frontend** in `../src-react/`.

- Still buildable via `npm run build:legacy` for reference
- **Do not add new features here**
- All active development happens in `src-react/`
